<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chrysus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".payment.ReceiveMoneyActivity"></activity>
        <activity android:name=".payment.SendMoneyActivity" />
        <activity android:name=".payment.NFCPayActivity" />
        <activity android:name=".payment.QRPayActivity" />
        <activity
            android:name=".util.MiddlewareActivity"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.login.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <meta-data android:name="news_url" android:value="http://198.12.106.137:5000/news/"/>
        <meta-data android:name="account_url" android:value="http://198.12.106.137:5000/payment/"/>
    </application>

</manifest>